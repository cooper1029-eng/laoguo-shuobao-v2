# Reasonix configuration.
# Resolution order: flag > ./reasonix.toml > ~/.config/reasonix/config.toml > built-in defaults.
# Secrets come from the environment via api_key_env; never put keys here.

config_version = 2   # schema marker for diagnostics; old versions may ignore it
default_model = "deepseek-flash"
# language      = "zh"   # ui/model language; empty = auto-detect from $LANG / $REASONIX_LANG

[agent]
# system_prompt = """..."""   # omit to use the built-in prompt for this version
# system_prompt_file = "prompts/system.md"   # overrides system_prompt when set
max_steps   = 0
temperature = 0.0
auto_plan   = "off"   # off|on; off keeps plan mode manual
# auto_plan_classifier = "deepseek-flash"   # optional; only used for borderline tasks
soft_compact_ratio  = 0.5   # notice only; keeps cache-first prefix intact
compact_ratio       = 0.8   # try compacting when prompt reaches this fraction
compact_force_ratio = 0.9   # force compacting at this high-water mark
# planner_model = "mimo"   # optional: enable two-model collaboration
# subagent_model = "deepseek-pro"   # optional default for runAs=subagent skills
# subagent_models = { review = "deepseek-pro", security_review = "deepseek-pro" }   # per-skill overrides
# subagent_effort = "high"   # optional default effort for subagents
# subagent_efforts = { review = "max", task = "high" }   # per-tool/skill effort overrides
# output_style = "explanatory"   # explanatory | learning | concise | custom; empty = default

[tools]
enabled = []   # empty = all built-in tools
bash_timeout_seconds = 120   # foreground safety cap; set 0 for no tool-local cap

[codegraph]
enabled      = true   # built-in MCP server; off by default for first-run sessions
auto_install = true   # fetch the runtime when CodeGraph is enabled but missing
# path       = ""   # empty = cache, then PATH, then a bundle beside reasonix

[skills]
# paths = ["~/my-skills", "../shared/skills"]   # extra custom skill roots
# excluded_paths = ["~/.agents/skills"]   # hide convention roots without deleting folders
# max_depth = 3   # nested scan depth; set 1 for legacy root-only discovery
# disabled_skills = ["review"]   # hide noisy or unwanted skills

[permissions]
# Per-call gating. mode = writer fallback when no rule matches: ask|allow|deny.
# Readers always default to allow. Precedence: deny > ask > allow > fallback.
# Rules are "ToolName" or "ToolName(glob)"; '*' matches any run, '?' one char.
mode  = "ask"
# deny = ["bash(rm -rf*)", "bash(git push*)"]   # hard-blocked in every mode
allow = ["bash=find /Users/guoxiang -maxdepth 1 -type d | head -20 && ls -la /Users/guoxiang/ | head -40", "bash=ls -la /Users/guoxiang/.reasonix/ && cat /Users/guoxiang/.reasonix/*.yaml 2>/dev/null || cat /Users/guoxiang/.reasonix/*.json 2>/dev/null || echo \"checking other formats...\" && ls -la /Users/guoxiang/.reasonix/", "bash=pwd && ls -la", "run_skill", "bash=cd /Users/guoxiang/Desktop/老郭网页版V2 && git status && echo \"---\" && git diff --stat && echo \"---\" && git log --oneline -5", "edit_file=/Users/guoxiang/Desktop/老郭网页版V2/js/app.js", "bash=cd /Users/guoxiang/Desktop/老郭网页版V2 && git add js/app.js && git commit -m \"增强文章生成prompt：注入完整知识库(zitao+covered)+示例文章+历史修改反馈+强化system message人设\"", "bash=cd /Users/guoxiang/Desktop/老郭网页版V2 && git log -1 --stat", "bash=cd /Users/guoxiang/Desktop/老郭网页版V2 && git add js/app.js && git commit -m \"增强标题生成prompt：注入风格指南，要求标题多样化(文化/实用/情感/悬念)并体现匠人名字\"", "bash=cd /Users/guoxiang/Desktop/老郭网页版V2 && git add js/app.js && git commit -m \"添加反馈闭环：保存修改意见到localStorage并在下次生成时注入；增强步骤4 rewrite prompt注入知识库\"", "bash=cd /Users/guoxiang/Desktop/老郭网页版V2 && git diff --stat HEAD~3..HEAD", "bash=cd /Users/guoxiang/Desktop/老郭网页版V2 && git diff HEAD~3..HEAD", "bash=cd /Users/guoxiang/Desktop/老郭网页版V2 && node -e \"\nconst fs = require('fs');\nconst code = fs.readFileSync('js/config.js','utf8') + '\\n' +\n             fs.readFileSync('js/llm.js','utf8') + '\\n' +\n             fs.readFileSync('js/vision.js','utf8') + '\\n' +\n             fs.readFileSync('js/topics.js','utf8') + '\\n' +\n             fs.readFileSync('js/app.js','utf8');\ntry {\n  new Function(code);\n  console.log('✅ 所有 JS 文件语法检查通过');\n} catch(e) {\n  console.log('❌ 语法错误:', e.message);\n  console.log('位置:', e.stack?.split('\\\\n').slice(0,3).join('\\\\n'));\n}\n\"", "bash=cd /Users/guoxiang/Desktop/老郭网页版V2 && git add js/app.js && git commit -m \"修复：补上 buildArticlePrompt() 中遗漏的 }, 闭合\"", "bash=cd /Users/guoxiang/Desktop/老郭网页版V2 && git log --oneline && echo \"---\" && git diff --stat HEAD~4..HEAD", "bash=cd /Users/guoxiang/Desktop/老郭网页版V2 && node -e \"\n// 验证 Key Functions Exist\nconst fs = require('fs');\nconst code = fs.readFileSync('js/config.js','utf8') + '\\n' +\n             fs.readFileSync('js/llm.js','utf8') + '\\n' +\n             fs.readFileSync('js/vision.js','utf8') + '\\n' +\n             fs.readFileSync('js/topics.js','utf8') + '\\n' +\n             fs.readFileSync('js/app.js','utf8');\nconst src = code;\nconst checks = [\n  ['getFewShotArticles', '✅ getFewShotArticles()'],\n  ['getEditFeedback', '✅ getEditFeedback()'],\n  ['_autoSaveFeedback', '✅ _autoSaveFeedback()'],\n  ['saveEditFeedback', '✅ saveEditFeedback()'],\n  ['KNOWLEDGE_BASE.zitao', '✅ zitao in buildArticlePrompt'],\n  ['KNOWLEDGE_BASE.covered', '✅ covered in buildArticlePrompt'],\n  ['KNOWLEDGE_BASE.style', '✅ style in generateTitles'],\n  ['我是老郭，讲述有宝物的故事，和有故事的宝物', '✅ 结尾固定句'],\n  ['禁止使用破折号', '✅ 禁止破折号'],\n];\nlet allOk = true;\nfor (const [check, label] of checks) {\n  if (src.includes(check)) {\n    console.log(label);\n  } else {\n    console.log('❌ MISSING:', check);\n    allOk = false;\n  }\n}\nif (allOk) console.log('\\\\n🎉 所有关键功能验证通过！');\n\"", "bash=cd /Users/guoxiang/Desktop/老郭网页版V2 && git log --oneline --stat -4", "bash=cd /Users/guoxiang/Desktop/老郭网页版V2 && git show 728abd7 --stat", "bash=cd /Users/guoxiang/Desktop/老郭网页版V2 && git show d740ce4 --stat", "bash=cd /Users/guoxiang/Desktop/老郭网页版V2 && git show cbf0354 --stat", "bash=cd /Users/guoxiang/Desktop/老郭网页版V2 && git diff --stat HEAD~4..HEAD && echo \"---\" && git log --oneline", "bash=cd /Users/guoxiang/Desktop/老郭网页版V2 && node -e \"\nconst fs = require('fs');\nconst code = fs.readFileSync('js/config.js','utf8') + '\\n' +\n             fs.readFileSync('js/llm.js','utf8') + '\\n' +\n             fs.readFileSync('js/vision.js','utf8') + '\\n' +\n             fs.readFileSync('js/topics.js','utf8') + '\\n' +\n             fs.readFileSync('js/app.js','utf8');\nnew Function(code);\nconst checks = [\n  ['getFewShotArticles', 'getFewShotArticles()'],\n  ['getEditFeedback', 'getEditFeedback()'],\n  ['_autoSaveFeedback', '_autoSaveFeedback()'],\n  ['saveEditFeedback', 'saveEditFeedback()'],\n  ['KNOWLEDGE_BASE.zitao', 'zitao in buildArticlePrompt'],\n  ['KNOWLEDGE_BASE.covered', 'covered in buildArticlePrompt'],\n  ['KNOWLEDGE_BASE.style', 'style in generateTitles'],\n  ['我是老郭，讲述有宝物的故事', '结尾固定句'],\n  ['禁止使用破折号', '禁止破折号'],\n];\nfor (const [check, label] of checks) {\n  if (code.includes(check)) console.log('✅', label);\n  else console.log('❌ MISSING:', check);\n}\nconsole.log('\\\\n🎉 全部验证通过');\n\"", "bash=cd /Users/guoxiang/Desktop/老郭网页版V2 && git show 51ce6e2 --stat", "bash=cd /Users/guoxiang/Desktop/老郭网页版V2 && node -e \"\nconst fs = require('fs');\nconst code = fs.readFileSync('js/config.js','utf8') + '\\n' +\n             fs.readFileSync('js/llm.js','utf8') + '\\n' +\n             fs.readFileSync('js/vision.js','utf8') + '\\n' +\n             fs.readFileSync('js/topics.js','utf8') + '\\n' +\n             fs.readFileSync('js/app.js','utf8');\nnew Function(code);\nconst checks = [\n  ['getFewShotArticles', 'getFewShotArticles()'],\n  ['getEditFeedback', 'getEditFeedback()'],\n  ['_autoSaveFeedback', '_autoSaveFeedback()'],\n  ['saveEditFeedback', 'saveEditFeedback()'],\n  ['KNOWLEDGE_BASE.zitao', 'zitao in buildArticlePrompt'],\n  ['KNOWLEDGE_BASE.covered', 'covered in buildArticlePrompt'],\n  ['我是老郭，讲述有宝物的故事', '结尾固定句'],\n  ['禁止使用破折号', '禁止破折号'],\n];\nfor (const [check, label] of checks) {\n  if (code.includes(check)) console.log('✅', label);\n  else console.log('❌ MISSING:', check);\n}\nconsole.log('');\nconsole.log('✅ JS 语法检查通过');\n\"", "bash=cd /Users/guoxiang/Desktop/老郭网页版V2 && git diff --stat HEAD~4..HEAD", "bash=cd /Users/guoxiang/Desktop/老郭网页版V2 && git log --oneline"]
# ask = ["write_file"]   # force a prompt even if otherwise allowed

[sandbox]
# Confine tool blast radius. File-writers (write_file/edit_file/multi_edit)
# may only write under workspace_root (empty = current dir) + allow_write.
# bash = "enforce" (default) jails each command in an OS sandbox (macOS now;
# graceful fallback elsewhere); "off" disables it. network allows egress.
# workspace_root = ""            # default: current working directory
# allow_write = ["/tmp"]          # extra dirs writers may also modify
bash    = "enforce"
network = true

[statusline]
# A custom status line: a command whose first stdout line replaces the built-in
# data row. It receives {"model","contextUsed","contextWindow","cwd"} as JSON on stdin.
# command = "my-statusline.sh"

# External MCP servers. type: "stdio" (default, a subprocess) | "http" | "sse".
# ${VAR} / ${VAR:-default} are expanded from the environment in command/args/env/url/headers.
# [[plugins]]
# name    = "example"
# command = "reasonix-plugin-example"
# [[plugins]]                                  # a remote server over Streamable HTTP
# name    = "stripe"
# type    = "http"
# url     = "https://mcp.stripe.com"
# headers = { Authorization = "Bearer ${STRIPE_KEY}" }
